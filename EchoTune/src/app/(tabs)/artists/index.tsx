import { defaultStyles } from "@/styles";
import React from "react";
import { Text, View } from "react-native";

const ArtistsScreen = () => {
  return (
    <View style={defaultStyles.container}>
      <Text style={defaultStyles.text}>Atists Screen</Text>
    </View>
  );
};

export default ArtistsScreen;
